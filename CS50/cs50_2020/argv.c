#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    if (argc == 3)
    {
        printf("hello, %s\n", argv[0]);
    }
    else
    {
        printf("hello, world\n");
    }
}