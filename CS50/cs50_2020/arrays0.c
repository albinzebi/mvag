#include <stdio.h>

int main(void)
{
    bool truthtable[] = { false, true, true };

    // or

    bool truthtable[3];
    truthtable[0] = false;
    truthtable[1] = true;
    truthtable[2] = true;
}