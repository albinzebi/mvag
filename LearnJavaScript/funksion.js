"use strict";
/*
async function shfaq() {
    let mbajFjalen = new Promise(function (pranoj, refuzoj) {
        setTimeout(function() { pranoj("hell on earth!!!"); }, 4000);
    });
    document.getElementById("sheno4").innerHTML = await mbajFjalen;
}

shfaq();
*/

var weekD = ['mon', 'tue', 'wed', 'thur', 'fri', 'sat', 'sun'];

for (var day of weekD)
{
    console.log(day);
}